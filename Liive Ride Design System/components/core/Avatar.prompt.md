First-line one-sentence: A circular driver/rider avatar that shows a photo when available and initials otherwise, with an optional accent ring for the active voice speaker.

```jsx
<Avatar name="Alice Nguyen" size={56} ring />
<Avatar src="/driver.jpg" name="John Driver" />
```

`ring` draws the accent speaking-indicator ring; `ringColor` overrides it.
