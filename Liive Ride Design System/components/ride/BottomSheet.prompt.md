First-line one-sentence: The iOS bottom sheet that rises over the map — rounded top corners, grabber handle, opaque sheet surface.

```jsx
<BottomSheet>
  <h2 className="t-title2">Choose your ride</h2>
  …
</BottomSheet>
```

The primary content container for every step of the ride flow. Pin a primary `Button` (capsule, fullWidth) at the bottom of its contents.
