First-line one-sentence: A map pin/marker for the live ride map — the accent car (driver), green origin dot, red destination pin, and orange transfer point.

```jsx
<MapMarker kind="car" label="4 min" />
<MapMarker kind="destination" label="Union Square" />
<MapMarker kind="transfer" label="Transfer" />
```

Each marker carries its own white outline + drop shadow so it reads against any map tile.
