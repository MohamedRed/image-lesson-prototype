First-line one-sentence: The multi-leg journey tracker — numbered leg circles joined by transfer (swap) links, with completed legs green and the current leg accent.

```jsx
<ProgressDots legs={3} current={2} />
```

Used inside the live-ride multi-leg panel. `current` past `legs` reads as journey complete.
